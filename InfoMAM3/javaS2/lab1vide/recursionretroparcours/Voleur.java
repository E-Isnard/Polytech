package lab1vide.recursionretroparcours;

 class Voleur {
    private int[] p;  // p[i] = le poids de l'objet i (p[i] > 0 pour tout i).
    private int[] v;  // v[i] = la valeur de l'objet i.
    
     Voleur(int[] p, int[] v) {
        this.p = p;
        this.v = v;
    }

    /**
     * Retourne la valeur maximale du butin que le voleur peut emporter
     * sans dépasser le poids total pmax.
     * Complexité : O(2^n) où n = T.length.
     */
     int valeur(int pmax) {
        return 0;
    }

    /**
     * Retourne la valeur maximale du butin que le voleur peut emporter
     * sans dépasser le poids total Pmax et en choisissant parmis les objets
     * de numéro [0..k].
     * Complexité : O(2^k).
     */
     int valeur(int Pmax, int k) {
    	return 0;
    }

}

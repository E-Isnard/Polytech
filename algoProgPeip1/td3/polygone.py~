from turtle import *

l =int(input("Longueur des cotes ?: "))
n = int(input("Combien de cote ?: "))
def polygone(n,l):
        for i in range(n):
                forward(l)
                left(360/n)


polygone(n,l)


def polygone(n,l):
        for i in range(n):
                forward(l)
                left(360/n)
